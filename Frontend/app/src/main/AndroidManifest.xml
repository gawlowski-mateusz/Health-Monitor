<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="nodomain.freeyourgadget.gadgetbridge.PERMISSION_RECEIVE_HR_DATA" />
    <uses-permission android:name="nodomain.freeyourgadget.gadgetbridge.PERMISSION_SEND_HR_DATA" />
    <uses-permission android:name="android.permission.BROADCAST_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BROADCASTS" />
    <uses-permission android:name="nodomain.freeyourgadget.gadgetbridge.permission.READ_DATA" />

    <queries>
        <package android:name="nodomain.freeyourgadget.gadgetbridge" />
        <provider android:authorities="nodomain.freeyourgadget.gadgetbridge.provider" />
        <provider android:authorities="nodomain.freeyourgadget.gadgetbridge" />
    </queries>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Frontend"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Frontend">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add this inside your application tag -->
        <receiver
            android:name=".HeartRateReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="TODO"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="nodomain.freeyourgadget.gadgetbridge.ACTION_HEARTRATE" />
                <action android:name="nodomain.freeyourgadget.gadgetbridge.ACTION_REALTIME_HEARTRATE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>